# Backend API URL. Required for iOS Simulator (use your Mac's IP, e.g. from `ipconfig getifaddr en0`).
# Leave unset for web or when backend is on same host.
# EXPO_PUBLIC_API_BASE_URL=http://10.89.14.163:8000
